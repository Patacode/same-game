/**
 * Package containing all the application controllers. The controllers are used
 * as intermediary between the views and models to transmit informations
 * and invoke the corresponding methods.
 */
package g56080.same.controller;
